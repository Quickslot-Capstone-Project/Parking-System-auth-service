name: Auth Service CI/CD

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Production release version, for example v1.0.0"
        required: true
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  ci:
    name: Auth Service CI
    if: ${{ github.event_name == 'pull_request' || github.event_name == 'push' }}
    uses: Quickslot-Capstone-Project/Parking-System/.github/workflows/reusable-ci.yml@main
    with:
      trigger_event: ${{ github.event_name }}
      source_sha: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}
      service_name: auth-service
      image_name: auth-service
      helm_values_key: authService
      docker_context: ./auth-service
      dockerfile_path: ./auth-service/Dockerfile
      working_directory: ./auth-service
      sonar_key: Quickslot-Capstone-Project_Parking-System-auth-service
    secrets: inherit

  release:
    name: Release Auth Service
    if: ${{ github.event_name == 'workflow_dispatch' }}
    uses: Quickslot-Capstone-Project/Parking-System/.github/workflows/reusable-release.yml@main
    with:
      release_tag: ${{ inputs.release_tag }}
      service_name: auth-service
      image_name: auth-service
      helm_values_key: authService
    secrets: inherit
